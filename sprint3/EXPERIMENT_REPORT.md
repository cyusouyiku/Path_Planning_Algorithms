# Sprint 3：大规模栅格路径规划对比实验报告

## 1. 实验目的

在 Sprint 2 五种算法（**Dijkstra、A\*、RRT\*、D\* Lite、PPO**）基础上，
将环境改为 **船舶管道模拟布局**（大矩形舱室块 + 外板/贴墙障碍），
在 **100×100 / 500×500 / 1000×1000** 三张固定 preset 上对比路径代价、扩展量与运行时间。

PPO 先在**随机地图池**上训练（详见 [PPO_TRAINING_REPORT.md](PPO_TRAINING_REPORT.md)），
再以权重 `outputs/models/ppo_random.zip` 在三张 benchmark 图上评测。

## 2. 环境与障碍说明

基础环境为 `pathplan/env/grid.py` 的 `GridEnvironment`，布局逻辑见 `pathplan/env/ship_pipe.py`。

| 规则 | 说明 |
|------|------|
| 大矩形块 | 随机放置舱室/设备矩形障碍 |
| 贴墙加密 | 外轮廓舱壁、障碍周边加厚 |
| 主通道 | **不设空旷走廊**（`corridor_half_width: 0`），舱室可阻断起终点连线 |
| 难度约束 | `min_path_stretch`：最短路径须明显绕障 |
| 安全区 | 起终点 `safe_radius` 邻域内不置障 |

移动模型：**八连通**（直行 1，对角 √2，不切角）。配置见 `configs/env_presets.yaml`。

### 2.1 三种预设环境

**ship_pipe_100**（100×100 船舶管道：舱室块可阻断主通道，需绕障）

- 规模：**100×100**
- 障碍格：**2216**（占比 **22.2%**）
- 起终点：`[5, 5]` → `[94, 94]`

**ship_pipe_500**（500×500 船舶管道：中等尺度，主通道有障碍）

- 规模：**500×500**
- 障碍格：**70000**（占比 **28.0%**）
- 起终点：`[25, 25]` → `[474, 474]`

**ship_pipe_1000**（1000×1000 船舶管道：大规模，需绕开舱室块）

- 规模：**1000×1000**
- 障碍格：**275166**（占比 **27.5%**）
- 起终点：`[50, 50]` → `[949, 949]`

## 3. 对比算法简述

| 算法 | 类型 | 说明 |
|------|------|------|
| Dijkstra | 图最短路 | 全局最优；1000×1000 为控时跳过。 |
| A* | 启发式搜索 | 八距离启发，扩展量通常远小于 Dijkstra。 |
| RRT* | 随机采样 | 多次采样取最优代价。 |
| D* Lite | 增量最短路 | 静态图与最短路同价；大规模可能跳过。 |
| PPO | 强化学习 | 21×21 局部视野 + 势函数塑形；**随机地图池训练**后固定图评测。 |

## 4. 实验设置

- **PPO 训练**：随机地图池课程（`ppo_random_curriculum.yaml`），总步数 **500,000**，耗时 **2424.7 s**。训练细节见 [PPO_TRAINING_REPORT.md](PPO_TRAINING_REPORT.md)。
- **PPO 评测权重**：`outputs/models/ppo_random.zip`

| 阶段 | 地图 | 障碍占比 | 步数 | 耗时 (s) |
|------|------|----------|------|----------|
| random_96 | 96×96 | 24.6% | 80000 | 52.3 |
| random_192 | 192×192 | 20.3% | 100000 | 66.9 |
| random_384 | 384×384 | 22.0% | 120000 | 86.8 |
| random_500 | 500×500 | 25.8% | 200000 | 155.2 |

- **经典算法**：各 preset 运行一次；RRT\* 多次采样取最优。
- **PPO**：单次 rollout + 15 回合 eval 成功率。
- **原始数据**：`outputs/results/benchmark_results.json`。

## 5. 实验结果

下表来自本次 benchmark（固定三张 preset + 训练后的 PPO）。

### 5.1 ship_pipe_100（100×100）

| 方法 | 成功 | 路径代价 | 路径长度（格子数） | 扩展量 / 采样 | 时间 (s) |
|------|------|----------|-------------------|---------------|----------|
| Dijkstra | 是 | 145.782 | 124 | 7690 | 0.098 |
| A* | 是 | 145.782 | 124 | 1469 | 0.020 |
| D* Lite | 是 | 145.782 | 124 | 2977 | 0.372 |
| RRT* | 是 | 157.723 | 126 | 791 | 1.150 |
| PPO | 否 (0% eval) | — | 401 | 400 | 0.206 |

- **经典最优代价**（A*）：**145.782**。
- **RRT\***：5 次运行取最优代价，代价 157.723。
- **PPO**：未到达终点（eval **0%**）。 步数上限内路径长 401。

### 5.2 ship_pipe_500（500×500）

| 方法 | 成功 | 路径代价 | 路径长度（格子数） | 扩展量 / 采样 | 时间 (s) |
|------|------|----------|-------------------|---------------|----------|
| Dijkstra | 是 | 712.004 | 573 | 177934 | 2.516 |
| A* | 是 | 712.004 | 573 | 51306 | 0.771 |
| D* Lite | 是 | 712.004 | 573 | 68192 | 9.540 |
| RRT* | 是 | 799.207 | 596 | 18000 | 86.947 |
| PPO | 否 (0% eval) | — | 2001 | 2000 | 0.964 |

- **经典最优代价**（A*）：**712.004**。
- **RRT\***：3 次运行取最优代价，代价 799.207。
- **PPO**：未到达终点（eval **0%**）。 步数上限内路径长 2001。

### 5.3 ship_pipe_1000（1000×1000）

| 方法 | 成功 | 路径代价 | 路径长度（格子数） | 扩展量 / 采样 | 时间 (s) |
|------|------|----------|-------------------|---------------|----------|
| Dijkstra | — | — | — | — | 地图边长 1000>600，为控制耗时跳过 |
| A* | 是 | 1422.511 | 1158 | 198598 | 2.861 |
| D* Lite | 是 | 1422.511 | 1158 | 225610 | 30.616 |
| RRT* | 是 | 2134.720 | 1596 | 12000 | 112.054 |
| PPO | 否 (0% eval) | — | 4001 | 4000 | 2.037 |

- **经典最优代价**（A*）：**1422.511**。
- **RRT\***：2 次运行取最优代价，代价 2134.720。
- **PPO**：未到达终点（eval **0%**）。 步数上限内路径长 4001。

### 5.4 规模与耗时趋势（汇总）

| Preset | 障碍占比 | A* 扩展 | A* 时间 (s) | PPO eval | 备注 |
|--------|----------|---------|-------------|----------|------|
| ship_pipe_100 | 22.2% | 1469 | 0.020 | 0% | — |
| ship_pipe_500 | 28.0% | 51306 | 0.771 | 0% | — |
| ship_pipe_1000 | 27.5% | 198598 | 2.861 | 0% | Dijkstra 跳过 |

## 6. 小结与讨论

1. **经典算法**：Dijkstra / A\* / D\* Lite 在静态八连通权下最优代价一致；A\* 扩展量与耗时通常最优。
2. **规模**：1000×1000 上 Dijkstra 跳过；A\* 仍可作大规模基线。
3. **RRT\***：离散栅格上多次采样后代价常高于最短路。
4. **PPO**：在随机地图池上训练后于**固定 preset** 评测，反映跨图泛化；
   与全局最优搜索比的是「学会导航」而非毫秒级查询。
5. 早期未训好的 `ppo_curriculum` 结果已由本次 **`ppo_random`** 权重复测替换。

## 7. 复现实验

```bash
cd sprint3
pip install -r requirements.txt
python scripts/train_ppo_random.py
python scripts/generate_ppo_training_report.py
python scripts/run_benchmark.py --model outputs/models/ppo_random.zip
python scripts/generate_report.py
```

分尺度 benchmark：`python scripts/run_benchmark.py --presets ship_pipe_100 --model outputs/models/ppo_random.zip`

## 8. 实验总结

本实验在 Sprint 2 五种算法框架下，将障碍布局升级为**船舶管道模拟**（矩形舱室 + 贴墙加密、主通道可阻断），并在 **100 / 500 / 1000** 三种栅格尺度上完成横向对比。环境与算法实现均基于八连通、不切角代价模型，三张 benchmark 图由固定 seed 生成，保证经典算法与 PPO 评测可复现。

**经典规划算法**方面，结论清晰且与理论一致：在可达静态图上，Dijkstra、A\*、D\* Lite 给出**相同最优路径代价**；A\* 凭借启发式在扩展节点数与 wall time 上整体最优（例如 500×500 扩展约 **5.1×10⁴**，毫秒～秒级）。RRT\* 在离散栅格上虽能求可行路，但多次采样后代价仍高于最短路约 **12%～50%**，更适合作为连续空间参考而非本场景主基线。规模放大至 1000×1000 时，Dijkstra 因耗时被跳过，**A\*** 仍是实用的大规模单次查询方案。

**强化学习（PPO）**方面，本批次采用**随机地图池课程**（约 50 万环境步、`ppo_random.zip`）后在三张**固定 preset** 上评测。训练留出集在小尺度随机图上约 **50%** 成功率（见 [PPO_TRAINING_REPORT.md](PPO_TRAINING_REPORT.md)），但固定 benchmark 上 **三档均为 0%**——说明策略尚未泛化到本实验指定的三张难图，且大图课程阶段基本未学会。主要原因包括：21×21 局部观测难以支撑长距离绕舱室、固定图与训练池分布差异、以及评测步数上限易在未到达终点时被截断。因此，**不宜将当前 PPO 与 A\* 在最优代价上直接类比**；PPO 更应视为「在部分随机同分布图上具备导航雏形、尚未完成固定 benchmark」的阶段性结果。

**综合建议**：（1）若工程目标是**静态已知地图上的最优或近优路径**，应优先采用 **A\***（或等价最短路）；（2）若需评估 RL，应先在与 preset 同分布的地图池上把成功率训稳，再并入本报告第五节主表；（3）船舶管道布局通过 `min_path_stretch` 避免了「对角线穿空」的过简实例，使经典算法对比更能反映**绕障规划**难度，达到了 Sprint 3 环境设计目标。

本实验完整数据见 `outputs/results/benchmark_results.json`；PPO 训练过程与留出评测见 `outputs/results/ppo_random_train.json` 及专项报告。后续可在改进观测视野、步数预算与课程设计后，用同一 benchmark 流程更新第五节 PPO 行，无需改动经典算法部分。

